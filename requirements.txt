# Runtime dependencies for the AgentArts container image.
# Mirrors the [project].dependencies of pyproject.toml, but pulls agentarts-sdk
# from PyPI instead of the local editable path used for development.
agentarts-sdk==0.1.6
langgraph>=1.0.0
langchain>=1.0.0
langchain-core>=1.0.0
langchain-openai>=1.0.0
python-dotenv>=1.0.0
httpx>=0.25.0
